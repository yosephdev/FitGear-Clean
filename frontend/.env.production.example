REACT_APP_API_URL=https://api.example.com
REACT_APP_NAME=FitGear

# Notes:
# - Set REACT_APP_API_URL to the base URL of your backend API (no trailing slash).
#   Example: https://api.fitgear.com or https://fit-gear-backend.vercel.app
# - After setting the environment variable in Vercel, redeploy the frontend project
#   so the value is baked into the production build.
